---
wave: 1
depends_on: []
files_modified: [package.json, tailwind.config.ts, app/layout.tsx, components/theme-provider.tsx, components/ui/mode-toggle.tsx]
autonomous: true
requirements: [UI-01]
---

# Phase 1: Plan 1 — General Project & UI Theming Setup

<objective>
Initialize the Next.js project with Tailwind CSS, Shadcn UI, and configure the dark/light mode toggle to satisfy UI-01.
</objective>

<tasks>
<task>
  <description>Create Next.js project and setup Shadcn UI</description>
  <read_first>
    - .planning/phases/01-foundation-theming/01-UI-SPEC.md
  </read_first>
  <action>
    Run `npx -y create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`
    Run `npx -y shadcn@latest init -d`
    Install `lucide-react` and `framer-motion` for our animations and icons.
  </action>
  <acceptance_criteria>
    - `package.json` contains `next`, `tailwindcss`, `lucide-react`, and `framer-motion`
    - `components.json` is generated
  </acceptance_criteria>
</task>

<task>
  <description>Implement Dark/Light Mode toggle</description>
  <read_first>
    - .planning/phases/01-foundation-theming/01-UI-SPEC.md
  </read_first>
  <action>
    Install `next-themes`.
    Create `src/components/theme-provider.tsx` that exports `ThemeProvider` wrapping `next-themes`.
    Wrap `src/app/layout.tsx` children in `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>`.
    Add Shadcn standard dark mode toggle `src/components/mode-toggle.tsx` containing Sun and Moon icons from `lucide-react`.
  </action>
  <acceptance_criteria>
    - `src/components/theme-provider.tsx` exists and exports ThemeProvider.
    - `src/app/layout.tsx` contains `<ThemeProvider>` wrapping the `<main>` content.
    - `src/components/mode-toggle.tsx` exists.
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
grep "next-themes" package.json
grep "ThemeProvider" src/app/layout.tsx
```
</verification>

<must_haves>
- The project runs via `npm run dev` locally and displays the mode toggle.
- Theme successfully switches `dark` class on the `<html>` element.
</must_haves>
