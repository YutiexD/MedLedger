---
wave: 2
depends_on: [01-01]
files_modified: [src/models/User.ts, src/app/api/auth/[...nextauth]/route.ts, src/components/ui/animated-characters-login-page.tsx, src/app/login/page.tsx, package.json]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
---

# Phase 2: Plan 2 — Authentication Backend & Login Page Integration

<objective>
Setup Mongoose User schema, API routes for login/registration, and implement the animated login page with MetaMask integration.
</objective>

<tasks>
<task>
  <description>Create User Schema and Registration API</description>
  <read_first>
    - src/lib/mongodb.ts
  </read_first>
  <action>
    Install `bcryptjs`.
    Create `src/models/User.ts` containing the User schema with `email`, `password` (hashed), and `walletAddress` fields.
    Create `src/app/api/auth/register/route.ts` to handle new user registration.
    Create `src/app/api/auth/login/route.ts` to handle standard credentials login.
  </action>
  <acceptance_criteria>
    - `src/models/User.ts` contains the User schema.
    - API endpoints `/api/auth/register` and `/api/auth/login` exist.
  </acceptance_criteria>
</task>

<task>
  <description>Add Shadcn UI components for Login</description>
  <read_first>
    - package.json
  </read_first>
  <action>
    Run `npx shadcn@latest add input label checkbox`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/input.tsx`, `src/components/ui/label.tsx`, and `src/components/ui/checkbox.tsx` exist.
  </acceptance_criteria>
</task>

<task>
  <description>Implement the Animated Login Page with MetaMask</description>
  <read_first>
    - .planning/phases/02-authentication-landing/02-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/ui/animated-characters-login-page.tsx` and paste the provided code for the animated form logic.
    Modify the component to handle state for "Connect MetaMask".
    Integrate `ethers` or `viem` if necessary to detect `window.ethereum`.
    Create `src/app/login/page.tsx` rendering the login component.
  </action>
  <acceptance_criteria>
    - `src/components/ui/animated-characters-login-page.tsx` exists.
    - `/login` page renders the animated form.
    - User can successfully register and login with ID/Password (stored in MongoDB).
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
ls src/models/User.ts
grep "User" src/app/api/auth/login/route.ts
grep "animated-characters-login-page" src/app/login/page.tsx
```
</verification>

<must_haves>
- User can successfully link their MetaMask wallet address during session.
- The login page includes tracking character eyes that follow input focus/selection (satisfies AUTH-03).
</must_haves>
