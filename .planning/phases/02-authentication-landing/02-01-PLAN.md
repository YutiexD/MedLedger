---
wave: 1
depends_on: []
files_modified: [src/components/ui/shape-landing-hero.tsx, src/app/page.tsx]
autonomous: true
requirements: [UI-02]
---

# Phase 2: Plan 1 — Landing Page & Hero Section

<objective>
Implement the primary landing page using the provided geometric hero component to satisfy UI-02.
</objective>

<tasks>
<task>
  <description>Create the Shape Landing Hero component</description>
  <read_first>
    - .planning/phases/02-authentication-landing/02-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/ui/shape-landing-hero.tsx` and paste the provided code for `ElegantShape` and `HeroGeometric`.
    Ensure `framer-motion` and `lucide-react` are usable.
  </action>
  <acceptance_criteria>
    - `src/components/ui/shape-landing-hero.tsx` exists and exports `HeroGeometric`.
  </acceptance_criteria>
</task>

<task>
  <description>Update Home Page to use HeroGeometric</description>
  <read_first>
    - src/app/page.tsx
  </read_first>
  <action>
    Modify `src/app/page.tsx` to import and render `HeroGeometric`.
    Pass props: `badge="MedLedger"`, `title1="Secure Blockchain"`, `title2="Hospital Bookings"`.
    Include a "Get Started" link pointing to `/login`.
  </action>
  <acceptance_criteria>
    - `src/app/page.tsx` renders `HeroGeometric`.
    - Landing page is visually consistent with the UI-SPEC.
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
ls src/components/ui/shape-landing-hero.tsx
grep "HeroGeometric" src/app/page.tsx
```
</verification>

<must_haves>
- The landing page displays the geometric shapes and animations.
- The "Get Started" button is visible and functional.
</must_haves>
