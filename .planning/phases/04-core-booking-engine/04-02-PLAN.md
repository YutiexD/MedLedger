---
wave: 2
depends_on: [04-01]
files_modified: [src/app/api/slots/book/route.ts, src/app/api/slots/cancel/route.ts, src/components/ui/sonner.tsx, src/app/dashboard/page.tsx, src/app/layout.tsx]
autonomous: true
requirements: [BOOK-06, UI-03]
---

# Phase 4: Plan 2 — Web3 Booking Engine & Real-time Notifications

<objective>
Wire up the frontend transactions with MetaMask, update the MongoDB status on confirmation, and provide Etherscan proofs with optimistic UI notifications.
</objective>

<tasks>
<task>
  <description>Add Sonner Toast Notifications</description>
  <read_first>
    - package.json
  </read_first>
  <action>
    Run `npx shadcn@latest add sonner`.
    Wrap the `ThemeProvider` in `src/app/layout.tsx` with `<Toaster />`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/sonner.tsx` exists.
    - Toaster is globally available in the Root Layout.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Dashboard & Booking Actions</description>
  <read_first>
    - src/app/search/page.tsx
  </read_first>
  <action>
    Create `src/app/dashboard/page.tsx`.
    Show "Current Bookings" with Etherscan badges.
    In `DoctorCard` or a central utility, implement the Web3 function `bookSlot(slotId)`. 
    - Trigger MetaMask `sendTransaction` to the `HospitalMedLedger` contract.
    - Wait for block confirmation.
    - On success, call `/api/slots/book` (new route) to set `status: "BOOKED"`, `patientId`, and `txHash`.
    - Trigger `toast.success("Booking Confirmed!")`.
  </action>
  <acceptance_criteria>
    - `/dashboard` correctly lists the user's current booked slots with links to Etherscan.
    - UI correctly reflects "BOOKED" status after transaction confirmation.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Slot Cancellation</description>
  <read_first>
    - src/app/dashboard/page.tsx
  </read_first>
  <action>
    Add a "Cancel Appointment" button to the dashboard slots.
    - Trigger `cancelSlot(slotId)` transaction in MetaMask.
    - On confirmation, call `/api/slots/cancel` (new route) to reset `status: "AVAILABLE"` and remove `patientId` and `txHash`.
    - Trigger `toast.info("Appointment Cancelled.")`.
  </action>
  <acceptance_criteria>
    - Appointments can be cancelled and slots returned to "AVAILABLE" in the search results.
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
grep "Toaster" src/app/layout.tsx
ls src/app/api/slots/book/route.ts
grep "sepolia.etherscan.io" src/app/dashboard/page.tsx
```
</verification>

<must_haves>
- Patients see a real-time toast notification for every core action (booking/cancellation).
- Etherscan links point to the correct Sepolia network based on the transaction hash.
- Slots are correctly reset in MongoDB only after the on-chain cancellation succeeds.
</must_haves>
