---
wave: 2
depends_on: [03-01]
files_modified: [src/app/api/doctors/search/route.ts, src/app/search/page.tsx, src/components/ui/doctor-card.tsx]
autonomous: true
requirements: [BOOK-03]
---

# Phase 3: Plan 2 — Advanced Search & Filter Experience

<objective>
Implement the primary doctor search API and a dedicated `/search` page for advanced filtering by specialization.
</objective>

<tasks>
<task>
  <description>Create Doctor Search API</description>
  <read_first>
    - src/models/User.ts
    - src/models/Slot.ts
  </read_first>
  <action>
    Create `src/app/api/doctors/search/route.ts`.
    Implement `GET` logic that accepts `specialization` and searches for Users with `role: "DOCTOR"` AND the specified specialization.
    Include available slots for each doctor found.
    Handle "all" or multiple specializations if possible.
  </action>
  <acceptance_criteria>
    - `/api/doctors/search` returns the correct doctor list with their current `AVAILABLE` slots.
  </acceptance_criteria>
</task>

<task>
  <description>Add Doctor Card UI</description>
  <read_first>
    - src/components/ui/button.tsx
  </read_first>
  <action>
    Create `src/components/ui/doctor-card.tsx` with a clean design, name, specialization, and slot list for selection.
    Use Shadcn UI for the UI components.
  </action>
  <acceptance_criteria>
    - `src/components/ui/doctor-card.tsx` correctly displays doctor information and shows a list of slots.
  </acceptance_criteria>
</task>

<task>
  <description>Develop Advanced Search Page</description>
  <read_first>
    - .planning/phases/03-doctor-availability-search/03-CONTEXT.md
  </read_first>
  <action>
    Create `src/app/search/page.tsx`.
    Include a Sidebar with a checklist of "Common Specializations" (Cardiology, Neurology, Pediatrics, General Practice, Dermatology).
    Render the list of `DoctorCard` components based on the search result.
    Support date selection if possible for further filtering.
  </action>
  <acceptance_criteria>
    - `/search` page allows filtering by specialization and date.
    - Each doctor card is correctly rendered with its current `AVAILABLE` 15-min slots.
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
grep "DOCTOR" src/app/api/doctors/search/route.ts
ls src/components/ui/doctor-card.tsx
grep "search" src/app/search/page.tsx
```
</verification>

<must_haves>
- Patients can successfully list and filter doctors based on their specified specialization.
- Doctors who are not verified via the blockchain role (from Phase 1/2) are correctly excluded from search results if not in the DB.
</must_haves>
