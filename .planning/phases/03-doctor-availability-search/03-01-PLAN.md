---
wave: 1
depends_on: [02-02]
files_modified: [src/models/Slot.ts, src/app/api/slots/route.ts, src/middleware/blockchain-auth.ts]
autonomous: true
requirements: [BOOK-02]
---

# Phase 3: Plan 1 — Slot Model & Posting System (15-min Intervals)

<objective>
Setup the Slot Mongoose model and create an authenticated API route that allows doctors to post 15-minute intervals verified against the smart contract.
</objective>

<tasks>
<task>
  <description>Create Slot Model</description>
  <read_first>
    - src/models/User.ts
  </read_first>
  <action>
    Create `src/models/Slot.ts` with `doctorId` (Ref User), `startTime` (Date), `endTime` (Date), `status` (Enum: AVAILABLE, BOOKED, CANCELLED), and `patientId` (Ref User, optional).
  </action>
  <acceptance_criteria>
    - `src/models/Slot.ts` exists and includes the required fields.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Blockchain-based Role Verification Middleware</description>
  <read_first>
    - contracts/HospitalMedLedger.sol
    - hardhat.config.ts
  </read_first>
  <action>
    Install `ethers`.
    Create `src/middleware/blockchain-auth.ts` containing the `verifyDoctorRole` function that checks the `HospitalMedLedger` contract on Sepolia using a provider and the user's `walletAddress`.
  </action>
  <acceptance_criteria>
    - `src/middleware/blockchain-auth.ts` contains the logic for `hasRole(DOCTOR_ROLE, address)`.
  </acceptance_criteria>
</task>

<task>
  <description>Create Slot Posting API with Auto-splitting</description>
  <read_first>
    - src/models/Slot.ts
  </read_first>
  <action>
    Create `src/app/api/slots/route.ts`.
    Implement `POST` function that:
    1. Authenticates the doctor (via blockchain-auth middleware logic).
    2. Takes a `startTime` and an `endTime`.
    3. Calculates 15-minute intervals and creates a `Slot` document for each interval in the database.
  </action>
  <acceptance_criteria>
    - `/api/slots` POST correctly creates multiple 15-min slots for a given time range.
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
grep "startTime" src/models/Slot.ts
ls src/middleware/blockchain-auth.ts
grep "15" src/app/api/slots/route.ts
```
</verification>

<must_haves>
- Slots are correctly split into 15-minute increments based on the doctor's input range.
- Slot posting is blocked if the user's wallet address does not hold the `DOCTOR_ROLE` on-chain.
</must_haves>
