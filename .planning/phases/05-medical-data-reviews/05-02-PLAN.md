---
wave: 2
depends_on: [05-01]
files_modified: [src/models/Review.ts, src/app/api/reviews/route.ts, src/app/prescription/[id]/page.tsx, src/components/ui/reviews-list.tsx]
autonomous: true
requirements: [MED-03, MED-04]
---

# Phase 5: Plan 2 — Patient Portal: Viewing, Downloading (PDF-Alternative) & Reviews

<objective>
Enable patients to view and print their prescriptions and implement a verified review system for doctor ratings.
</objective>

<tasks>
<task>
  <description>Create Review Model</description>
  <read_first>
    - src/models/User.ts
  </read_first>
  <action>
    Create `src/models/Review.ts` with `doctorId` (Ref User), `patientId` (Ref User), `rating` (Number, 1-5), and `comment` (String).
  </action>
  <acceptance_criteria>
    - `src/models/Review.ts` exists and includes the required fields.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Review eligibility and GET/POST API</description>
  <read_first>
    - src/models/Slot.ts
  </read_first>
  <action>
    Create `src/app/api/reviews/route.ts`.
    Implement `POST` function that:
    1. Authenticates the patient.
    2. Verifies the patient has a common history (past slot) with the doctor.
    3. Saves the review.
    Implement `GET` logic that:
    1. Fetches all reviews for a given `doctorId`.
  </action>
  <acceptance_criteria>
    - `/api/reviews` correctly filters so only patients with a past slot can leave a review.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Print-friendly Prescription Page</description>
  <read_first>
    - src/app/layout.tsx
  </read_first>
  <action>
    Create `src/app/prescription/[id]/page.tsx`.
    Styled with `@media print { .no-print { display: none; } }`.
    Show doctor details, specialization, medicine list with dosage, and digital watermark.
    Include a "Print" button calling `window.print()`.
  </action>
  <acceptance_criteria>
    - Patients can view and trigger a printable version of their prescription.
  </acceptance_criteria>
</task>

<task>
  <description>Develop Reviews UI Component</description>
  <read_first>
    - src/components/ui/doctor-card.tsx
  </read_first>
  <action>
    Create `src/components/ui/reviews-list.tsx`.
    Show stars and comment text.
    Include the patient's full name as per user request.
    Integrate this component into the `DoctorCard` or Search result profile details.
  </action>
  <acceptance_criteria>
    - Public reviews are visible to all users on the doctor's profile/search card.
  </acceptance_criteria>
</task>
</tasks>

<verification>
```bash
grep "rating" src/models/Review.ts
ls src/app/prescription/[id]/page.tsx
grep "window.print" src/app/prescription/[id]/page.tsx
```
</verification>

<must_haves>
- Review system only allows users with a past, BOOKED appointment (Slot startTime < NOW) to submit feedback.
- Publicly visible names are displayed for each review as per Phase 5 context decisions.
</must_haves>
